import styled from 'styled-components/native'
export const VideoContainer = styled.View`
	flex: 1;
	background-color: #fafafafa;
	align-items: center;
	justify-content: center;
`;

export const VideoControl = styled.View`
	position: absolute;
	bottom: 0;
	left: 0;
	right: 0;
	height: 45;
	flex-direction: row;
	align-items: center;
	justify-content: center;
	background-color: rgba(0, 0, 0, 0.5);
`;

